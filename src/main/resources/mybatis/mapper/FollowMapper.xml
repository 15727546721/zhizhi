<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xu.infrastructure.persistent.dao.FollowMapper">
    <resultMap id="BaseResultMap" type="cn.xu.infrastructure.persistent.po.Follow">
        <id column="id" property="id"/>
        <result column="follower_id" property="followerId"/>
        <result column="followed_id" property="followedId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id
        , follower_id, followed_id, status, create_time, update_time
    </sql>

    <insert id="insert" parameterType="cn.xu.infrastructure.persistent.po.Follow">
        INSERT INTO `follow` (follower_id, followed_id, status)
        VALUES (#{followerId}, #{followedId}, #{status})
    </insert>

    <update id="updateStatus">
        UPDATE `follow`
        SET status = #{status}
        WHERE follower_id = #{followerId}
          AND followed_id = #{followedId}
    </update>

    <select id="getByFollowerAndFollowed" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `follow`
        WHERE follower_id = #{followerId} AND followed_id = #{followedId}
    </select>

    <select id="listByFollowerId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `follow`
        WHERE follower_id = #{followerId} AND status = 1
        ORDER BY create_time DESC
    </select>

    <select id="listByFollowedId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `follow`
        WHERE followed_id = #{followedId} AND status = 1
        ORDER BY create_time DESC
    </select>

    <select id="countFollowing" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM `follow`
        WHERE follower_id = #{followerId}
          AND status = 1
    </select>

    <select id="countFollowers" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM `follow`
        WHERE followed_id = #{followedId}
          AND status = 1
    </select>
    <select id="findStatus" resultType="java.lang.Integer">
        SELECT status
        FROM `follow`
        WHERE follower_id = #{followerId}
          AND followed_id = #{followedId}
    </select>
    
    <select id="findById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `follow`
        WHERE id = #{id}
    </select>
    
    <delete id="deleteById">
        DELETE FROM `follow`
        WHERE id = #{id}
    </delete>
    
    <delete id="deleteByIds">
        DELETE FROM `follow`
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    
    <select id="listFollowingByPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `follow`
        WHERE follower_id = #{followerId} AND status = 1
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="listFollowersByPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `follow`
        WHERE followed_id = #{followedId} AND status = 1
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="findMutualFollows" resultType="java.lang.Long">
        SELECT f1.followed_id
        FROM `follow` f1
        INNER JOIN `follow` f2 ON f1.followed_id = f2.follower_id AND f1.follower_id = f2.followed_id
        WHERE f1.follower_id = #{userId} AND f1.status = 1 AND f2.status = 1
        ORDER BY f1.create_time DESC
        LIMIT #{offset}, #{limit}
    </select>
</mapper>