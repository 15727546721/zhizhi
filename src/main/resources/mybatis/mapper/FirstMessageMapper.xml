<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xu.infrastructure.persistent.dao.FirstMessageMapper">
    
    <resultMap id="BaseResultMap" type="cn.xu.infrastructure.persistent.po.FirstMessage">
        <id column="id" property="id"/>
        <result column="sender_id" property="senderId"/>
        <result column="receiver_id" property="receiverId"/>
        <result column="message_id" property="messageId"/>
        <result column="has_replied" property="hasReplied"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    
    <sql id="Base_Column_List">
        id, sender_id, receiver_id, message_id, has_replied, create_time, update_time
    </sql>
    
    <insert id="insert" parameterType="cn.xu.infrastructure.persistent.po.FirstMessage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `first_message` (sender_id, receiver_id, message_id, has_replied, create_time, update_time)
        VALUES (#{senderId}, #{receiverId}, #{messageId}, #{hasReplied}, #{createTime}, #{updateTime})
    </insert>
    
    <update id="update" parameterType="cn.xu.infrastructure.persistent.po.FirstMessage">
        UPDATE `first_message`
        SET has_replied = #{hasReplied},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>
    
    <select id="selectBySenderAndReceiver" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `first_message`
        WHERE sender_id = #{senderId} AND receiver_id = #{receiverId}
        LIMIT 1
    </select>
    
    <update id="updateHasReplied">
        UPDATE `first_message`
        SET has_replied = #{hasReplied},
            update_time = NOW()
        WHERE sender_id = #{senderId} AND receiver_id = #{receiverId}
    </update>
    
    <select id="existsBySenderAndReceiver" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM `first_message`
        WHERE sender_id = #{senderId} AND receiver_id = #{receiverId}
    </select>
</mapper>

