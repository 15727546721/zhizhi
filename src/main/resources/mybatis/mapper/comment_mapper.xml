<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xu.infrastructure.persistent.dao.ICommentDao">
    <resultMap id="BaseResultMap" type="cn.xu.infrastructure.persistent.po.Comment">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="type" property="type" jdbcType="INTEGER" typeHandler="cn.xu.infrastructure.persistent.handler.CommentTypeHandler"/>
        <result column="target_id" property="targetId" jdbcType="BIGINT"/>
        <result column="parent_id" property="parentId" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="reply_user_id" property="replyUserId" jdbcType="BIGINT"/>
        <result column="content" property="content" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, type, target_id, parent_id, user_id, reply_user_id, content, create_time, update_time
    </sql>

    <insert id="insert" parameterType="cn.xu.infrastructure.persistent.po.Comment" useGeneratedKeys="true"
            keyProperty="id">
        insert into comment (type, target_id, parent_id, user_id, reply_user_id, content, create_time, update_time)
        values (#{type}, #{targetId}, #{parentId}, #{userId}, #{replyUserId}, #{content}, #{createTime},
                #{updateTime})
    </insert>

    <delete id="deleteById">
        delete from comment where id = #{id}
    </delete>

    <select id="findById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from comment
        where id = #{id}
    </select>

    <select id="findByTypeAndTargetId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM comment
        WHERE type = #{type}
        AND target_id = #{targetId}
        ORDER BY create_time DESC
    </select>

    <select id="findByParentId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM comment
        WHERE parent_id = #{parentId}
        ORDER BY create_time DESC
    </select>

    <!-- 批量删除评论 -->
    <delete id="batchDelete" parameterType="java.util.List">
        DELETE FROM comment
        WHERE id IN
        <foreach collection="commentIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteByTypeAndTargetId" parameterType="map">
        DELETE FROM comment
        WHERE type = #{type} AND target_id = #{targetId}
    </delete>

    <!-- 查询一级评论列表 -->
    <select id="findRootComments" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM comment
        WHERE target_id = #{targetId}
        AND parent_id IS NULL
        <if test="type != null">
            AND type = #{type}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 分页查询二级评论列表 -->
    <select id="findRepliesByPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM comment
        WHERE parent_id = #{parentId}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 分页查询一级评论列表 -->
    <select id="findRootCommentsByPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM comment
        WHERE parent_id IS NULL
        <if test="type != null">
            AND type = #{type}
        </if>
        <if test="targetId != null">
            AND target_id = #{targetId}
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 统计一级评论总数 -->
    <select id="countRootComments" resultType="long">
        SELECT COUNT(*)
        FROM comment
        WHERE parent_id IS NULL
        <if test="type != null">
            AND type = #{type}
        </if>
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
    </select>

    <!-- 根据ID查询评论 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM comment
        WHERE id = #{id}
    </select>
    
    <!-- 根据父评论ID删除所有子评论 -->
    <delete id="deleteByParentId">
        DELETE FROM comment 
        WHERE parent_id = #{parentId}
    </delete>

    <!-- 根据父评论ID列表批量查询子评论 -->
    <select id="findByParentIds" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM comment
        WHERE parent_id IN
        <foreach collection="parentIds" item="parentId" open="(" separator="," close=")">
            #{parentId}
        </foreach>
        ORDER BY create_time ASC
    </select>

    <!-- 根据父评论ID删除所有子评论 -->
</mapper>
