<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xu.infrastructure.persistent.dao.IArticleDao">

    <resultMap id="BaseResultMap" type="cn.xu.infrastructure.persistent.po.Article">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="description" column="description" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="coverUrl" column="cover_url" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="BIGINT"/>
        <result property="viewCount" column="view_count" jdbcType="BIGINT"/>
        <result property="collectCount" column="collect_count" jdbcType="BIGINT"/>
        <result property="likeCount" column="like_count" jdbcType="BIGINT"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="publishTime" column="publish_time" jdbcType="TIMESTAMP"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <resultMap id="ArticleListResultMap" type="cn.xu.api.web.model.vo.article.ArticleListVO">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="coverUrl" column="cover_url"/>
        <result property="viewCount" column="view_count"/>
        <result property="likeCount" column="like_count"/>
        <result property="createTime" column="create_time"/>
        <result property="publishTime" column="publish_time"/>
        <result property="nickname" column="nickname"/>

        <!-- 映射 Tag 集合 -->
        <collection property="tagNameList" ofType="String">
            <result property="name" column="name"/>
        </collection>
    </resultMap>

    <select id="queryByCategoryId" parameterType="java.lang.Long" resultMap="ArticleListResultMap">
        SELECT DISTINCT
        a.id,
        a.title,
        a.description,
        a.cover_url,
        a.user_id,
        a.view_count,
        a.like_count,
        a.create_time,
        a.publish_time,
        u.nickname,
        t.name
        FROM article a
        JOIN user u ON a.user_id = u.id
        <if test="categoryId != null">
            JOIN article_category_relation ac ON a.id = ac.article_id
        </if>
        LEFT JOIN article_tag_relation at ON a.id = at.article_id
        LEFT JOIN article_tag t ON at.tag_id = t.id
        WHERE a.status = 1
        <if test="categoryId != null">
            AND ac.category_id = #{categoryId}
        </if>
    </select>

    <sql id="Base_Column_List">
        id
        ,title,content,
        cover_url,user_id,
        view_count,collect_count,
        like_count,create_time,update_time
    </sql>

    <insert id="insert" parameterType="cn.xu.infrastructure.persistent.po.Article" useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO article (title, description, content, cover_url, user_id,
                             view_count, collect_count, like_count, status, publish_time, create_time, update_time)
        VALUES (#{title}, #{description}, #{content}, #{coverUrl}, #{userId},
                #{viewCount}, #{collectCount}, #{likeCount}, #{status}, now(), now(), now())
    </insert>
    <update id="update">
        UPDATE article
        SET title         = #{title},
            description   = #{description},
            content       = #{content},
            cover_url     = #{coverUrl},
            user_id       = #{userId},
            view_count    = #{viewCount},
            collect_count = #{collectCount},
            like_count    = #{likeCount},
            status        = #{status},
            publish_time  = now(),
            update_time   = now()
        WHERE id = #{id}
    </update>
    <update id="updateStatus">
        UPDATE article
        SET status = #{status}
        WHERE id = #{id}
    </update>
    <update id="updateLikeCount">
        UPDATE article
        SET like_count = #{count}
        WHERE id = #{targetId}
    </update>

    <delete id="deleteByIds">
        DELETE FROM article WHERE id IN
        <foreach collection="articleIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteById">
        DELETE FROM article WHERE id = #{id}
    </delete>

    <select id="queryByPage" parameterType="cn.xu.api.system.model.dto.article.ArticleRequest"
            resultType="cn.xu.api.web.model.vo.article.ArticlePageVO">
        SELECT
        a.id,
        a.title,
        a.description,
        a.cover_url AS coverUrl,
        a.create_time AS createTime,
        a.update_time AS updateTime,
        c.name AS categoryName,
        GROUP_CONCAT(t.name SEPARATOR ',') AS tagNames,
        u.nickname
        FROM
        article AS a
        JOIN article_category_relation AS ac ON a.id = ac.article_id
        JOIN article_category AS c ON ac.category_id = c.id
        LEFT JOIN article_tag_relation AS at ON a.id = at.article_id
        LEFT JOIN article_tag AS t ON at.tag_id = t.id
        LEFT JOIN user AS u ON a.user_id = u.id
        <where>
            <if test="title != null and title != ''">
                AND a.title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="categoryId != null">
                AND ac.category_id = #{categoryId}
            </if>
            <if test="tagId != null">
                AND EXISTS (SELECT 1 FROM article_tag_relation WHERE article_id = a.id AND tag_id = #{tagId})
            </if>
        </where>
        GROUP BY a.id, c.name, u.nickname
        ORDER BY a.create_time DESC
        LIMIT #{pageNo}, #{pageSize}
    </select>

    <select id="findById" resultType="cn.xu.infrastructure.persistent.po.Article" resultMap="BaseResultMap">
        SELECT id,
               title,
               description,
               content,
               cover_url,
               user_id,
               view_count,
               collect_count,
               like_count,
               create_time,
               update_time
        FROM article
        WHERE id = #{id}
    </select>
    <select id="queryArticleByPage" resultType="cn.xu.domain.article.model.entity.ArticleRecommendOrNew">
        SELECT a.id                                                         AS article_id,
               a.title,
               a.description,
               a.content,
               a.cover_url,
               a.user_id,
               a.view_count,
               a.collect_count,
               a.like_count,
               a.create_time,
               a.update_time,
               GROUP_CONCAT(DISTINCT t.name ORDER BY t.name SEPARATOR ', ') AS tags,
               u.nickname                                                   AS author_nickname,
               u.avatar                                                     AS author_avatar
        FROM article a
                 LEFT JOIN
             article_tag_relation at
        ON a.id = at.article_id
            LEFT JOIN
            article_tag t ON at.tag_id = t.id
            LEFT JOIN
            user u ON a.user_id = u.id
        GROUP BY
            a.id
        ORDER BY
            a.create_time DESC;
    </select>

    <!-- 分页查询文章列表 -->
    <select id="selectArticlePage" resultMap="ArticlePageResponseMap">
        SELECT
        a.id,
        a.title,
        a.cover_url,
        a.description,
        a.user_id,
        u.nickname,
        c.name as category_name,
        GROUP_CONCAT(DISTINCT t.name ORDER BY t.name SEPARATOR ',') as tag_names,
        a.create_time,
        a.update_time
        FROM article a
        LEFT JOIN user u ON a.user_id = u.id
        LEFT JOIN article_category_relation ac ON a.id = ac.article_id
        LEFT JOIN article_category c ON ac.category_id = c.id
        LEFT JOIN article_tag_relation at ON a.id = at.article_id
        LEFT JOIN article_tag t ON at.tag_id = t.id
        <where>
            <if test="title != null and title != ''">
                AND a.title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="categoryId != null">
                AND ac.category_id = #{categoryId}
            </if>
            <if test="tagId != null">
                AND at.tag_id = #{tagId}
            </if>
        </where>
        GROUP BY a.id, a.title, a.cover_url, a.description, a.user_id, u.nickname, c.name, a.create_time, a.update_time
        ORDER BY a.create_time DESC
        LIMIT #{offset}, #{size}
    </select>

    <!-- 文章列表响应结果映射 -->
    <resultMap id="ArticlePageResponseMap" type="cn.xu.api.web.model.vo.article.ArticlePageVO">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="coverUrl" column="cover_url"/>
        <result property="description" column="description"/>
        <result property="userId" column="user_id"/>
        <result property="nickname" column="nickname"/>
        <result property="categoryName" column="category_name"/>
        <result property="tagNames" column="tag_names"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findAllPublishedArticles" resultType="cn.xu.infrastructure.persistent.po.Article">
        SELECT id,
               title,
               description,
               content,
               cover_url,
               user_id,
               view_count,
               collect_count,
               like_count,
               create_time,
               update_time
        FROM article
        WHERE status = 1
        ORDER BY create_time DESC
    </select>

    <select id="findAll" resultType="cn.xu.infrastructure.persistent.po.Article">
        SELECT id,
               title,
               description,
               content,
               cover_url,
               user_id,
               view_count,
               collect_count,
               like_count,
               create_time,
               update_time
        FROM article
        ORDER BY create_time DESC
    </select>
    <!-- 查询已发布的文章列表 -->
    <select id="queryByUserId" parameterType="java.lang.Long" resultMap="ArticleListResultMap">
        SELECT DISTINCT
            a.id,
            a.title,
            a.description,
            a.cover_url,
            a.user_id,
            a.view_count,
            a.like_count,
            a.create_time,
            a.publish_time,
            u.nickname,
            t.name
        FROM article a
                 JOIN user u ON a.user_id = u.id
                 LEFT JOIN article_tag_relation at ON a.id = at.article_id
            LEFT JOIN article_tag t ON at.tag_id = t.id
        WHERE a.status = 1
          AND a.user_id = #{userId}
    </select>

    <!-- 查询草稿状态的文章列表 -->
    <select id="queryDraftArticleListByUserId" parameterType="java.lang.Long" resultMap="ArticleListResultMap">
        SELECT DISTINCT
            a.id,
            a.title,
            a.description,
            a.cover_url,
            a.user_id,
            a.view_count,
            a.like_count,
            a.create_time,
            a.publish_time,
            u.nickname,
            t.name
        FROM article a
                 JOIN user u ON a.user_id = u.id
                 LEFT JOIN article_tag_relation at ON a.id = at.article_id
            LEFT JOIN article_tag t ON at.tag_id = t.id
        WHERE a.status = 0
          AND a.user_id = #{userId}
    </select>

</mapper>
