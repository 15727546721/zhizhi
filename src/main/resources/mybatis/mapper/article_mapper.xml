<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xu.infrastructure.persistent.dao.IArticleDao">

    <resultMap id="BaseResultMap" type="cn.xu.infrastructure.persistent.po.Article">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="introduction" column="introduction" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="coverUrl" column="cover_url" jdbcType="VARCHAR"/>
        <result property="authorId" column="author_id" jdbcType="BIGINT"/>
        <result property="status" column="status" jdbcType="CHAR"/>
        <result property="viewCount" column="view_count" jdbcType="BIGINT"/>
        <result property="favoritesCount" column="favorites_count" jdbcType="BIGINT"/>
        <result property="likeCount" column="like_count" jdbcType="BIGINT"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id
        ,title,content,
        cover_url,author_id,
        status,
        view_count,favorites_count,
        like_count,create_time,update_time
    </sql>

    <insert id="insert" parameterType="cn.xu.infrastructure.persistent.po.Article" useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO article (title, introduction, content, cover_url, author_id, status,
                             view_count, favorites_count, like_count, create_time, update_time)
        VALUES (#{title}, #{introduction}, #{content}, #{coverUrl}, #{authorId}, #{status},
                #{viewCount}, #{favoritesCount}, #{likeCount}, now(), now())
    </insert>
    <update id="update">
        UPDATE article
        SET title           = #{title},
            introduction    = #{introduction},
            content         = #{content},
            cover_url       = #{coverUrl},
            author_id       = #{authorId},
            status          = #{status},
            view_count      = #{viewCount},
            favorites_count = #{favoritesCount},
            like_count      = #{likeCount},
            update_time     = now()
        WHERE id = #{id}
    </update>

    <delete id="deleteByIds">
        DELETE FROM article WHERE id IN
        <foreach collection="articleIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="queryByPage" parameterType="cn.xu.api.dto.article.ArticleRequest"
            resultType="cn.xu.api.dto.article.ArticlePageResponse">
        SELECT
        a.id,
        a.title,
        a.introduction,
        a.cover_url AS coverUrl,
        a.status,
        a.create_time AS createTime,
        c.name AS categoryName,
        GROUP_CONCAT(t.name SEPARATOR ',') AS tagNames
        FROM
        article AS a
        JOIN article_category AS ac ON a.id = ac.article_id
        JOIN category AS c ON ac.category_id = c.id
        LEFT JOIN article_tag AS at ON a.id = at.article_id
        LEFT JOIN tag AS t ON at.tag_id = t.id
        <where>
            <if test="title != null and title != ''">
                AND a.title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="status != null and status != ''">
                AND a.status = #{status}
            </if>
            <if test="categoryId != null">
                AND a.category_id = #{categoryId}
            </if>
            <if test="tagId != null">
                AND EXISTS (SELECT 1 FROM article_tag WHERE article_id = a.id AND tag_id = #{tagId})
            </if>
        </where>
        GROUP BY a.id, c.name
        ORDER BY a.create_time DESC
        LIMIT #{page}, #{size}
    </select>

    <select id="findById" resultType="cn.xu.infrastructure.persistent.po.Article" resultMap="BaseResultMap">
        SELECT id,
               title,
               introduction,
               content,
               cover_url,
               author_id,
               status,
               view_count,
               favorites_count,
               like_count,
               create_time,
               update_time
        FROM article
        WHERE id = #{id}
    </select>
</mapper>
