<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xu.infrastructure.persistent.dao.SystemConfigMapper">

    <resultMap id="BaseResultMap" type="cn.xu.infrastructure.persistent.po.SystemConfig">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="configKey" column="config_key" jdbcType="VARCHAR"/>
        <result property="configValue" column="config_value" jdbcType="VARCHAR"/>
        <result property="configDesc" column="config_desc" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, config_key, config_value, config_desc, create_time, update_time
    </sql>

    <insert id="insert" parameterType="cn.xu.infrastructure.persistent.po.SystemConfig" useGeneratedKeys="true" keyProperty="id">
        insert into system_config
        (config_key, config_value, config_desc, create_time, update_time)
        values
        (#{configKey}, #{configValue}, #{configDesc}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="cn.xu.infrastructure.persistent.po.SystemConfig">
        update system_config
        <set>
            <if test="configValue != null">config_value = #{configValue},</if>
            <if test="configDesc != null">config_desc = #{configDesc},</if>
            update_time = #{updateTime}
        </set>
        where id = #{id}
    </update>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM system_config
        WHERE config_key = #{configKey}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM system_config
        ORDER BY config_key
    </select>

    <select id="selectByKeyPrefix" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM system_config
        WHERE config_key LIKE CONCAT(#{keyPrefix}, '%')
        ORDER BY config_key
    </select>

    <delete id="deleteByKey">
        DELETE FROM system_config
        WHERE config_key = #{configKey}
    </delete>

</mapper>

