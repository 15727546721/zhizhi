<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xu.repository.mapper.PostTagMapper">

    <resultMap id="BaseResultMap" type="cn.xu.model.entity.PostTag">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="postId" column="post_id" jdbcType="BIGINT"/>
        <result property="tagId" column="tag_id" jdbcType="BIGINT"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, post_id, tag_id, create_time
    </sql>

    <insert id="insert">
        INSERT INTO post_tag (post_id, tag_id, create_time)
        VALUES (#{postId}, #{tagId}, now())
    </insert>

    <insert id="insertBatch">
        INSERT INTO post_tag (post_id, tag_id, create_time)
        VALUES
        <foreach collection="tagIds" item="tagId" separator=",">
            (#{postId}, #{tagId}, now())
        </foreach>
    </insert>
    
    <insert id="insertBatchByList">
        INSERT INTO post_tag (post_id, tag_id, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.postId}, #{item.tagId}, now())
        </foreach>
    </insert>
    
    <insert id="insertTags">
        INSERT INTO post_tag (post_id, tag_id, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.postId}, #{item.tagId}, now())
        </foreach>
    </insert>

    <select id="selectTagIdsByPostId" resultType="java.lang.Long">
        SELECT tag_id
        FROM post_tag
        WHERE post_id = #{postId}
    </select>

    <select id="selectPostIdsByTagId" resultType="java.lang.Long">
        SELECT post_id
        FROM post_tag
        WHERE tag_id = #{tagId}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="selectByPostIds" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM post_tag
        WHERE post_id IN
        <foreach collection="postIds" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
    </select>

    <delete id="deleteByPostId">
        DELETE FROM post_tag
        WHERE post_id = #{postId}
    </delete>

    <delete id="deleteByPostIdAndTagIds">
        DELETE FROM post_tag
        WHERE post_id = #{postId} AND tag_id IN
        <foreach collection="tagIds" item="tagId" open="(" separator="," close=")">
            #{tagId}
        </foreach>
    </delete>

    <delete id="deleteByTagId">
        DELETE FROM post_tag
        WHERE tag_id = #{tagId}
    </delete>
    
    <delete id="deleteByPostIds">
        DELETE FROM post_tag
        WHERE post_id IN
        <foreach collection="postIds" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
    </delete>

    <select id="countByTagId" resultType="int">
        SELECT COUNT(*)
        FROM post_tag
        WHERE tag_id = #{tagId}
    </select>

    <select id="selectHotTags" resultType="cn.xu.model.dto.post.TagStatistics">
        SELECT 
            t.id as tagId,
            t.name as tagName,
            COUNT(pt.tag_id) as usageCount
        FROM tag t
        LEFT JOIN post_tag pt ON t.id = pt.tag_id
        GROUP BY t.id, t.name
        ORDER BY usageCount DESC
        LIMIT #{limit}
    </select>

</mapper>